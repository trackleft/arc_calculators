/**
 * - Add row for the calculator.
 * - Submit calculator form.
 */

/**
 * Attaches behavior for GPA calculator.
 */
(($, Drupal) => {
  Drupal.behaviors.arcGPACalculator = {
    attach() {},
  };
})(jQuery, Drupal);
